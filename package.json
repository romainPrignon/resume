{
  "name": "resume",
  "version": "1.0.0",
  "description": "personnal jsonresume",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/romainPrignon/resume.git"
  },
  "main": "resume.json",
  "scripts": {
    "export": "resume export -t flat resume.raw.flat.pdf && resume export -t flat resume.raw.flat.html"
  },
  "dependencies": {
    "jsonresume-theme-short": "0.1.7",
    "resume-cli": "^1.1.2"
  }
}
